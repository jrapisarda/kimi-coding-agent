FROM python:3.12-slim
WORKDIR /app
COPY .. /app
RUN pip install --no-cache-dir fastapi uvicorn[standard]
CMD ["uvicorn", "codex_chopan_app.services.api_gateway.main:app", "--host", "0.0.0.0", "--port", "8000"]
